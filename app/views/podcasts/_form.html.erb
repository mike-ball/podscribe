<%= simple_form_for(@podcast) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :rss_feed %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
